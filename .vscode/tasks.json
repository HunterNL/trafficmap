{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run dev server",
            "type": "shell",
            "command": "go run ./cmd/devserver",
            "problemMatcher": [],
            "isBackground": true,
            "detail": "Run the dev server, serving local data in place of the live api",
            "promptOnClose": false,
            "runOptions": {
                "instanceLimit": 1,
                "runOn": "folderOpen"
            }
        }
    ]
}